<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="6dp"
    android:layout_marginBottom="6dp"
    app:cardBackgroundColor="@color/bg_surface"
    app:cardCornerRadius="14dp"
    app:strokeColor="@color/border"
    app:strokeWidth="1dp"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="14dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/txtHubName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/text_primary"
                android:textSize="15sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/txtHubSlug"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/text_secondary"
                android:textSize="12sp"
                android:layout_marginTop="2dp" />

        </LinearLayout>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnRenameHub"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:text="@string/hub_action_rename"
            android:textSize="12sp"
            android:layout_marginEnd="6dp"
            style="@style/Widget.EmergencyHub.Button.Secondary" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnDeleteHub"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:text="@string/hub_action_delete"
            android:textSize="12sp"
            style="@style/Widget.EmergencyHub.Button.Secondary" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
